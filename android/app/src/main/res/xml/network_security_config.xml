<?xml version="1.0" encoding="utf-8"?>
<!-- Android blocks cleartext (http://) traffic by default since API 28.
     Production always uses https (see ApiConfig's default), but local dev
     against a plain-http backend (API_SCHEME=http override, e.g. the
     Android emulator's 10.0.2.2 alias or a LAN IP) needs an explicit
     exemption — scoped to local/dev hosts only, not a blanket allow. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Testing on a physical device over LAN (API_HOST=192.168.x.x:5001)?
             Add your machine's LAN IP as another <domain> line here. -->
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
